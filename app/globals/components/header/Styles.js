import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  headerTitle: {
    fontSize: 24,
    fontWeight: "600",
  },

  headerDescription: {
    marginTop: 8,
    fontSize: 16,
    fontWeight: "400",
  },
});

export default styles;
