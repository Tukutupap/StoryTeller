import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  input: {
    height: 40,
    marginBottom: 15,
    borderWidth: 1,
    padding: 10,
  },
  inputError: {
    height: 40,
    marginBottom: 5,
    borderWidth: 1,
    padding: 10,
  },
  errorContainer: { height: 25, marginBottom: 1 },
  error: { color: "red", marginBottom: 10 },
});

export default styles;
